						August 20, 2009 

elf2bin is a utility to create a raw binary file from an ELF file.

To build this utility, invoke GNU "make" (or "gmake") in the source 
directory:

	cd <directory of elf2bin>
	gmake

To test:
	cd test
	gmake
